#include <stdio.h>
int prime(int a);

int main() {

    int n;
    printf("Enter your number:\n");
    scanf("%d",&n);

        if(prime(n)) {

            printf("%d Is prime number.", n);
        }

    else{
            printf("%d Is not prime number.",n);

    }
}
int prime(int i){

    int j;

    for(j=2;j<i;j++)
    {
        if(i%j==0)
            return 0;
    }
    return i;
}
